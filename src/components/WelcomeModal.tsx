
"use client";

// This component is currently disabled to favor a more robust music playback strategy
// that initiates on the first user interaction with the page.
export function WelcomeModal() {
  return null;
}
